import * as React from "react";
import { Routes, Route, Outlet, Link } from "react-router-dom";

export default function AppSettings() {
  return (
    <div>
      <h1>AppSettings page</h1>

    </div>
  );
}